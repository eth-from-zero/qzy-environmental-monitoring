import {
    HorizontalBox, LineEdit
} from "std-widgets.slint";

TextLineEdit := HorizontalLayout {
    property <string> text_text;
    property <string> line_edit_text;

    Text {
        text: root.text_text;
    }

    LineEdit {
        text: root.line_edit_text;
    }
}

