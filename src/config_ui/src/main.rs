use config_ui::ComponentHandle;
use config_ui::Main;
// use config_ui::TestMain;
// use config_ui::ui::other_page::OtherPage;

fn main() {
    let win = Main::new();

    // OtherPage::new(win);

    win.run();
}
