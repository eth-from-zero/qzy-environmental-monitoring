use iced::{button, Button, Column, Element, Sandbox, Settings, Text};

// use components::{TableView2, TableWidget, WRenderer};

// pub struct TestView {
//     table_view: TableView2<Message>,
// }

// #[derive(Debug, Clone)]
// pub enum Message {}

// impl Sandbox for TestView {
//     type Message = Message;

//     fn new<'a>(state: &'a mut iced::scrollable::State) -> Self {
//         Self {
//             table_view: TableView2::new(
//                 state,
//                 vec![TableWidget::new(Text::new("hello"), |w, e| {
//                     println!("{:?}", e);
//                 })],
//             ),
//         }
//     }

//     fn title(&self) -> String {
//         String::from("TableView2")
//     }

//     fn update(&mut self, message: Message) {}

//     fn view(&mut self) -> Element<Message> {
//         self.table_view.into()
//     }
// }

fn main() {
    // TestView::run(Settings::default());
}
